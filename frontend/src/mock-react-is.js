export const isFragment = (element) => false; export const ForwardRef = Symbol.for('react.forward_ref'); export const Memo = Symbol.for('react.memo'); export const isMemo = (element) => false; export const isForwardRef = (element) => false; export const typeOf = (element) => null; export const isElement = (element) => typeof element === 'object' && element !== null; export const isValidElementType = (type) => true; export const isContextConsumer = (element) => false; export const isContextProvider = (element) => false; export const isLazy = (element) => false; export const isPortal = (element) => false; export const isProfiler = (element) => false; export const isStrictMode = (element) => false; export const isSuspense = (element) => false;
